# 2026-04-22 - Frontend Developer

## [11:30] Вынесение налоговых констант из хардкода в шаблонах

- Что: создан `web/handlers/tax_display.go` со структурой `TaxConstants` и функцией `PrepareTaxConstants()`. Четыре info-страницы обновлены: хендлеры передают `PrepareTaxConstants()` вместо `nil`, шаблоны используют переменные.
- Результат: успех, `go build ./...` и `go test ./...` прошли чисто.
- Вывод:
  - Info-страницы (`special_tax_modes`, `regional_info`, `tax_deductions`, `employment_types`) передавали `nil` в шаблоны — при этом `{{.CurrentYear}}` в meta-тегах не рендерился. После добавления `CurrentYear int` в `TaxConstants` это тоже починилось.
  - Лимит вычета на детей — `450 000 ₽` (с 2025 года), не 350 000. В задании была опечатка, в шаблоне уже стояло правильное значение.
  - Второй ребёнок с 2025 года — `2 800 ₽` (в задании не упоминалось, но в шаблоне было). Добавил `ChildDeduction2` в структуру.
  - Третий ребёнок с 2025 года — `6 000 ₽` (повышен с 3 000). Добавил в структуру новое значение.
  - Примеры-расчёты типа `80 000 × 13% = 10 400 ₽` — не трогал по заданию, это иллюстративные числа.
