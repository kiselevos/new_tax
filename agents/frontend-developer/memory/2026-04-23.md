# 2026-04-23 - Frontend Developer

## [текущее время] SEO-правки во всех шаблонах

- Что: исправлены SEO-проблемы на всех страницах: canonical, OG-теги, noindex, sitemap.
- Результат: успех, `go build ./...` прошёл чисто.
- Вывод:
  - `result.tmpl` уже имел `lang="ru"` на `<html>` — добавлен только `<meta name="robots" content="noindex, nofollow">`.
  - JSON-LD в `index.tmpl` был с захардкоженным `2025` в поле `description` — заменён на `{{.CurrentYear}}`. Важно: Go шаблонные выражения работают внутри `<script type="application/ld+json">`, потому что это не `text/javascript`.
  - `og:image` должен быть абсолютным URL — поисковики и соцсети не разрешают относительные пути для этого тега.
  - Canonical теги добавляются перед закрывающим `</head>`, а не после `<link rel="stylesheet">` — структуру каждого шаблона надо проверить прежде чем вставить.
  - Шаблон `special_tax_modes.tmpl` имел двойной пустой отступ между keywords и stylesheet — один убран при вставке OG-блока.
  - `sitemap.xml` не содержал `/tax-deductions` и `/employment-types` — добавлены с приоритетами 0.7 и 0.6 соответственно (в соответствии с задачей).
