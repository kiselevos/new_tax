# 2026-09-04 - main

## [Политика конфиденциальности] Новая инфостраница /privacy
- Что: добавлена страница политики обработки ПД (152-ФЗ) — шаблон, хендлер, роут, футер, sitemap, тесты
- Результат: успех, go build + go test зелёные
- Вывод: чеклист для новой инфостраницы в web/: templates/<name>.tmpl (define + info_nav + footer) →
  handlers.go: Routes() + метод-хендлер → handlers_test.go: два списка (TestTemplatesRender, TestHandlers_StatusOK) →
  static/sitemap.xml → templates/footer.tmpl. Данные для страниц без расчёта передаются nil, значения берутся
  через template funcs из template_funcs.go (env + дефолт), а не через структуру.
- Специфика: на сайте нет cookies/аналитики/localStorage — это использовано как аргумент в тексте политики.
