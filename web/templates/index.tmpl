{{define "index"}}
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Расчёт НДФЛ</title>
  <link rel="stylesheet" href="/static/style.css">
  <script src="/static/script.js" defer></script>
</head>

<script>
window.APP_CONFIG = {
    minSalary: {{getMinSalary}},
};
</script>
<script src="/static/script.js" defer></script>

<body>
  <header>
    <h1>Калькулятор НДФЛ</h1>
  </header>

  <main>
    <form action="/calculate" method="post" class="form" 
      data-min-salary="{{getMinSalary}}">
      <!-- оклад -->
<div class="field-row">
    <div class="field-left">
        <div class="field-header">
            <div class="field-tooltip">
                <span class="tooltip-icon">?</span>
                <div class="tooltip-text">Укажите ваш оклад, указанный в трудовом договоре. Это сумма до вычета налогов и без надбавок.</div>
            </div>
            <label for="grossSalary">Оклад:</label>
        </div>
    </div>
    <div class="field-right">
        <div class="salary-field-container">
            <div class="salary-field">
                <input type="text" id="grossSalary" name="grossSalary" inputmode="decimal" required placeholder="Введите сумму">
                <span class="ruble">₽</span>
            </div>
            <!-- Сообщение об ошибке будет добавляться сюда -->
        </div>
    </div>
</div>

      <!-- территориальный коэффициент -->
      <div class="field-row">
        <div class="field-left">
          <div class="field-header">
            <div class="field-tooltip">
              <span class="tooltip-icon">?</span>
              <div class="tooltip-text">Устанавливается в зависимости от региона, где вы работаете. Например, для районов Крайнего Севера применяются повышающие коэффициенты.</div>
            </div>
            <label for="territorialMultiplier">Территориальный коэффициент:</label>
          </div>
          <a href="/regional-info" class="under-title">Подробнее →</a>
        </div>
        <div class="field-right">
          <select id="territorialMultiplier" name="territorialMultiplier" class="field-select">
            <option value="100" selected> -- без коэффициента -- </option>
            {{range .Territorial}}
              <option value="{{.Value}}">{{.Label}}</option>
            {{end}}
          </select>
        </div>
      </div>

      <!-- северная надбавка -->
      <div class="field-row">
        <div class="field-left">
          <div class="field-header">
            <div class="field-tooltip">
              <span class="tooltip-icon">?</span>
              <div class="tooltip-text">Северная надбавка зависит от региона и стажа работы. Начисляется постепенно с увеличением процента за каждый год работы.</div>
            </div>
            <label for="northernCoefficient">Северная надбавка:</label>
          </div>
          <a href="/regional-info" class="under-title">Подробнее →</a>
        </div>
        <div class="field-right">
          <select id="northernCoefficient" name="northernCoefficient" class="field-select">
            <option value="100" selected> -- без надбавки -- </option>
            {{range .Northern}}
              <option value="{{.Value}}">{{.Label}}</option>
            {{end}}
          </select>
        </div>
      </div>

      <!-- дата начала расчёта -->
      <div class="field-row">
        <div class="field-left">
          <div class="field-header">
            <div class="field-tooltip">
              <span class="tooltip-icon">?</span>
              <div class="tooltip-text">На этом этапе расчёт выполняется с 1-го числа выбранного месяца текущего года.</div>
            </div>
            <label>Дата начала расчёта:</label>
          </div>
        </div>
        <div class="field-right date-field">
          <span>01</span>
          <select name="startDate">
            {{range .Months}}
            <option value="{{.Value}}" {{if eq .Value "01"}}selected{{end}}>{{.Label}}</option>
            {{end}}
          </select>
          <span>{{.CurrentYear}}</span>
        </div>
      </div>

      <!-- чекбоксы -->
      <div class="field-row">
        <div class="field-left">
          <div class="field-header">
            <div class="field-tooltip">
              <span class="tooltip-icon">?</span>
              <div class="tooltip-text">Укажите льготы или ограничения, если они есть. Они повлияют на итоговый расчёт налогов.</div>
            </div>
            <label>Параметры:</label>
          </div>
          <a href="/special-tax-modes" class="under-title">Подробнее →</a>
        </div>
        <div class="field-right checkbox-column exclusive-checkboxes">
    <label class="checkbox-inline exclusive-option">
        <input type="checkbox" name="hasTaxPrivilege"> 
        <span>Льготы для силовых структур</span>
      </label>
      <label class="checkbox-inline exclusive-option">
        <input type="checkbox" name="isNotResident"> 
        <span>Налоговый нерезидент</span>
      </label>
      </div>
</div>

      <button type="submit">Рассчитать</button>

      <div class="calculator-note">
        <p>🔍 Все расчёты выполняются по открытым формулам и не сохраняются на сервере. Это инструмент для справочных целей, чтобы вы могли оценить свой доход «на руки».</p>
        <a href="/how-it-works" class="calculator-link">подробнее о работе калькулятора →</a>
      </div>
    </form>
  </main>

  <footer>
    <small>© {{.CurrentYear}} TaxCalc</small>
  </footer>
</body>
</html>
{{end}}